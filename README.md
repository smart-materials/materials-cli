# Materials CLI

This repository is a controlled Ring 3 evaluation project.

The project estimates material sheet counts and creates one JSON report.

Run these commands:

```bash
npm run lint
npm test
npm run artifact
```

The continuous integration workflow runs candidate code without network access.
It accepts pull requests from a same-repository `agent/<run_id>` branch only.

Validation canary: 2026-08-20.
